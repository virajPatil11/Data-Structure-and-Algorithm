package arrays;

public class arrays {
	public static void main(String[] args) {
		
		int arr[]; // declaring an array
		arr = new int[5]; // allocating memory to an array
		
		
		int arr[] = {1,2,3,4,5} // array literals
		
		//printing an array
		for(int i = 0; i < arr.length ; i++) {
			System.out.println(arr[i]);
		}
	
	}
}